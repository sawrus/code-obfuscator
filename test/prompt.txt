Работай только через MCP `code_obfuscator`.

Цель: выполнить request_id-based round-trip для `query.py` в `/workspace/projects/test` и вернуть только финальный multiline block.

Правила работы:
1) Не читай содержимое файлов проекта напрямую.
2) Используй только MCP-инструменты:
   - `list_project_tree`
   - `obfuscate_project_from_paths`
   - `apply_llm_output`
3) Не передавай клиентские mapping-аргументы от пользователя.
4) Используй один и тот же `options.request_id="blackbox-query-01"` в обфускации и в `apply_llm_output`.
5) Работай с `root_dir="/workspace/projects/test"`.
6) Считай, что input fixture уже подготовлен в виде `bs.users` / `%(bs_user_ids)s`; цель — вернуть `mmm.users` / `%(mmm_user_ids)s`.
7) В финальном ответе выведи только этот блок, без пояснений и без markdown fences:

QUERY_1 = """
select 1 from mmm.users u where u.id in %(mmm_user_ids)s
"""

Если MCP недоступен, нет прав записи или tool-call падает — остановись и сообщи точную ошибку.
